# 問題点と改善提案

## 実施済みの改善
- モジュール分割を実施: `app / camera / preferences / inference / pipeline / ui` の責務を分離。
- 設定永続化を実装: `ConfigStore` でカメラ選択とパイプライン設定を TOML に保存。
- 推論障害時フォールバックを実装: `HandTrackingProcessor` 初期化失敗時に `NoopProcessor` へ切替。
- 推論安定化を実装: ROI追跡、ランドマーク平滑化、検出妥当性チェックを導入。
- ジェスチャー入力を部分実装: ピンチ比率で左/右クリックを判定し、クリッククールダウンを導入。

## 残課題（次の改善候補）

1) ジェスチャー入力の拡張
  - クリックは実装済み。ドラッグ、スクロール、ホールドなどの連続操作を追加する。

2) モジュール依存方向の整理
  - `pipeline <-> inference` の相互参照を解消するため、`Frame` などの共有型を独立モジュールへ切り出す。

3) 推論スループットの最適化
  - 推論間隔・ROIサイズ・補間係数を環境別にチューニングし、FPSと操作遅延を両立する。

4) クロスプラットフォーム強化
  - Windows以外でもカーソル移動・クリック制御が有効になるようOS別バックエンドを実装する。

5) 自動テストの強化
  - 座標変換、ROI更新、検出妥当性判定、設定ロード/セーブのユニットテストを整備する。

## 実装チェックリスト（現時点）
- [x] カメラ列挙・選択・フレーム取得を実装
- [x] ONNX手ランドマーク推論を統合
- [x] 骨格描画とカーソル追従を実装
- [x] ピンチジェスチャーによる左/右クリックを実装
- [x] 設定永続化とフォールバック処理を実装
- [ ] ドラッグ・スクロール等の追加ジェスチャー
- [ ] 非Windowsのカーソル/クリック制御バックエンド
- [ ] テストコード整備（次フェーズ）
